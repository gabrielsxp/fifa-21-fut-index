import { Story, Meta } from '@storybook/react/types-6-0'
import HomeSearch from '.'

export default {
  title: 'HomeSearch',
  component: HomeSearch
} as Meta

export const Default: Story = () => <HomeSearch />
