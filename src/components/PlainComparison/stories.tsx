import { Story, Meta } from '@storybook/react/types-6-0'
import PlainComparison from '.'

export default {
  title: 'PlainComparison',
  component: PlainComparison
} as Meta

export const Default: Story = () => <PlainComparison />
