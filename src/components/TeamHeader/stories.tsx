import { Story, Meta } from '@storybook/react/types-6-0'
import TeamHeader from '.'

export default {
  title: 'TeamHeader',
  component: TeamHeader
} as Meta

export const Default: Story = () => <TeamHeader />
