import { Story, Meta } from '@storybook/react/types-6-0'
import TeamPlayers from '.'

export default {
  title: 'TeamPlayers',
  component: TeamPlayers
} as Meta

export const Default: Story = () => <TeamPlayers />
