import { Story, Meta } from '@storybook/react/types-6-0'
import TeamTemplate from '.'

export default {
  title: 'TeamTemplate',
  component: TeamTemplate
} as Meta

export const Default: Story = () => <TeamTemplate />
