import { Story, Meta } from '@storybook/react/types-6-0'
import PlayerTemplate from '.'

export default {
  title: 'PlayerTemplate',
  component: PlayerTemplate
} as Meta

export const Default: Story = () => <PlayerTemplate />
